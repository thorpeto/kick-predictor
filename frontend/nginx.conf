server {
    listen 80;
    
    # DNS resolver für Docker
    resolver 127.0.0.11 valid=30s;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    
    # In GCP kann die Backend-URL eine vollständige URL sein,
    # wenn Backend und Frontend getrennt laufen
    location /api {
        # Für lokale Entwicklung mit Docker Compose
        set $backend_url http://backend:8000;
        
        # Für GCP: Wenn BACKEND_URL definiert ist, verwende diese
        if ($http_x_backend_url) {
            set $backend_url $http_x_backend_url;
        }
        
        proxy_pass $backend_url;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Health check für GCP
    location /health {
        access_log off;
        return 200 "OK";
    }
}
